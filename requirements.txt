# Core API
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.0.0

# Database
aiosqlite==0.20.0
sqlalchemy==2.0.36

# Logging
structlog==24.4.0

# HTTP client (for pipeline emitter — httpx replaces blocking requests)
requests==2.32.3
httpx==0.27.2

# Redis Streams (event transport — optional, falls back to HTTP if unavailable)
redis>=5.0.0

# Vision pipeline (optional — comment out if installing separately)
# ultralytics>=8.0.0
# torch>=2.0.0
# torchvision>=0.15.0
opencv-python-headless==4.10.0.84

# Zone geometry
shapely==2.0.6

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Dashboard / terminal
rich==13.9.4
